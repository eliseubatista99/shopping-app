import { AppLayout, SignInOrLoginTemplate } from "@components";

export const SignInMobile: React.FC = () => {
  // const { i18n, form, onClickSubmit } = useSignInPageHelper();

  return (
    <AppLayout>
      <SignInOrLoginTemplate />
    </AppLayout>
  );
};
