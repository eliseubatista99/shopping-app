import { Modal } from "@eliseubatista99/react-scaffold-core";
import type { AppModalProps } from "./appModal";

export const AppModalMobile = (props: AppModalProps) => {
  return <Modal {...props}></Modal>;
};
